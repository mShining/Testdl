# Dockerfile to create a docker image
# Base image
#FROM golang:1.4.2

# Add project folder to the image
#ADD . $GOPATH/src

#ADD run.sh /run.sh

#RUN chmod +x /run.sh

# Expose the container port
#EXPOSE 8080

#WORKDIR $GOPATH/src

#CMD ["/run.sh"]
